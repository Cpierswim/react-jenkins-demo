pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "Building the applicaton..."'
            }
        }

        stage('Docker') {
            steps {
                sh 'echo "Dockerizing the applicaton..."'
            }
        }
    }
}